# CrossFX
Cross platform HTML/Javascript and Java application.
This project shows an example of a cross platform application developed in Java + HTML Javascript.

It is possible to use the same view (html) both in the JavaFX application, and as an external page, using the same Java code to handle the requests.
